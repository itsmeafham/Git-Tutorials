hello this my index file of version 3 in github 


Important commands in github
git --version
git status
git init
git add .
git config --global user.name "    "
git config --global user.email "    "
git log
git commit -m "message here"
git push -u origin master
git pull  origin master
git diff "page name"

#Setup Username and Email
git config --global user.name "itsmeafham"
git config --global user.email mafham0786@gmail.com

#Add remote origin
git remote add origin git@bitbucket.org:haris989/flaskapp-codewithharry.com.git

#Stage your changes commit and push
git add -A

#Commit your changes
git commit -m "commit message"

#Push your changes
git push -u origin master

making new branch
git branch (name of new branch)
 
git checkout (to  move bw branches)